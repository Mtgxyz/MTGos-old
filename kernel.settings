arch = x86
#MODE = debug
MODE = release # enables optimization

export PATH := $(HOME)/opt/bin:$(PATH)
#PREFIX =
PREFIX = i686-elf-

ifeq ($(MODE),debug)
  	CFLAGS += -g3 -DDEBUG
	CPPFLAGS += -g3 -DDEBUG
else
	CFLAGS += -O2
	CPPFLAGS += -O2
endif
